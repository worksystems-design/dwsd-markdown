# Local-only notes — never published.
/_local/

# Local-only Claude Code instructions — loaded locally, never committed.
/CLAUDE.local.md

# Editor / OS cruft — never published.
.obsidian/
.vscode/
.DS_Store
Thumbs.db
